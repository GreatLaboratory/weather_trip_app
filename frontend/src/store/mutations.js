export default {
    SET_LIST(state, list) {
        state.list = list;
    },
    SET_USER(state, user) {
        state.user = user;
    },
    SET_Item(state, item) {
        state.item = item;
    },
}